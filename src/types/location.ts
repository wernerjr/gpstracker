export interface LocationData {
    latitude: number;
    longitude: number;
    speed: number;
    timestamp: number;
  }
  
  // Se houver outras interfaces relacionadas à localização, podem ser adicionadas aqui